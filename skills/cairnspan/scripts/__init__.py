"""Cairnspan session-tooling scripts.

These modules are standalone, stdlib-only command-line scripts. They import
one another as flat top-level modules and are designed to be executed with
``python <script>.py ...`` from this directory (or through the ``cairnspan``
console dispatcher, which runs them the same way). They are not intended to
be imported as ``cairnspan.scripts.<module>``.
"""
