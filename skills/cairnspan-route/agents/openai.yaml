interface:
  display_name: "Cairnspan Route"
  short_description: "Choose one Cairnspan path and its gate"
  default_prompt: "Use $cairnspan-route to choose one Cairnspan path and report its required gate without launching it."
policy:
  allow_implicit_invocation: false
